results/

# Local data inputs (matrices, taxonomy, ontology) - never part of the repo
*.tsv
*.obo

# Bundled reference data (small, static, shipped with the repo)
!data/All_GOs_ic.tsv
!data/species_taxid.tsv
!data/species_lineage.tsv

# Generated outputs / build artifacts
test_*.html
__pycache__/
*.pyc

# NCBI taxdump cache (build_species_lineage.py) -- ~1.3 GB, re-downloaded/
# re-extracted on demand, never part of the repo
data/.ncbi_taxdump_cache/
